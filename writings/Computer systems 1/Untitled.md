---
id: Untitled
aliases: []
tags: []
Title:
date: 2026-08-10
---
![[Untitled-1786436654981.webp]]

We use base 10 systems cause we happen to have 10 fingers and it feels practical. But when counting in a computer with voltage values its easier to use base 2 or just on and off signals.(cause of voltages noive and stuff we cant represent 10 distinc voltage values in every circuit reliably).

Unsigned ints are numbers only >=0 and signed are all the numbers. Floating points numbers are base 2 version on scientific notaion. 
Overflow issues happen cause computers encord numbers in a limited amount of bits.

> I dont get floating point numbers yet.

So as we know programs see memory as a long array of bytes, this is called virtual memory and the each has a address. All the address are called the virual address space. So the memory that we know when we code, and what the program knows is false, there is no monolihic byte array.

>virtual address space is just a conceptual image presented to the machine-level program. The actual implementation (presented in Chapter 9) uses a combination of dynamic random access memory (DRAM), flash memory, disk storage, special hardware, and operating system software to provide the program with what appears to be a monolithic byte array. 

So everything in the program mostly happens in the address space with managing the memory.

Hexadecimal is cool.




